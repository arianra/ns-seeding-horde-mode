--[[ Horde Mode — HordeRegistry (i3a). Track every entity/bot we spawn
     (id->ref + kind index); BTC snapshot/restore. Source of truth for
     teardown + accounting (GetBotPlayerCount unreliable — spike tby). STUB. ]]
local M = {}
return M
