--[[ Horde Mode — takeover (i3b). Engage/Release the vanilla BotTeamController:
     DisableUpdate + SetMaxBots(0) on start; restore snapshot on teardown
     ("/horde IS the horde warmup" — Q7q7). STUB. ]]
local M = {}
return M
