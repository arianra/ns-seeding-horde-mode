--[[ Horde Mode — spawner (i4b mouths, i5a bots). SpawnMouth(point,wave):
     unpaired TunnelEntrance + SetConstructionComplete + HP scale + registry.
     SpawnBot(mouthPoint,techId): PlayerBot recipe (Initialize + lifeformEvolution
     + SetOrigin), materialization retry. DestroyMouth / bot teardown. STUB. ]]
local M = {}
return M
